#!/bin/bash

DB_FILE="storage.db"

# Проверка: существует ли база данных
# Смотри задания тута:   https://docs.google.com/document/d/1HTJQ8QaDV1WNj_JcSdu62ZJJvmGZMzyCKzePJ7Nm7Tw/edit?usp=sharing
if [[ ! -f "$DB_FILE" ]]; then
    echo "📂 Файл базы данных '$DB_FILE' не найден. Создаю..."
    sqlite3 "$DB_FILE" "" || { echo "❌ Ошибка при создании базы данных."; exit 1; }
else
    echo "📁 Используем существующую базу данных '$DB_FILE'"
fi

# Выполнение SQL скрипта (создание таблиц + вставка данных)
sqlite3 "$DB_FILE" <<'EOF'
-- ========== СОЗДАНИЕ ТАБЛИЦ ==========

CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    email TEXT NOT NULL UNIQUE,
    pass_hash BLOB NOT NULL,
    total_attempts INTEGER DEFAULT 0,
    pass_levels INTEGER DEFAULT 0
);

CREATE INDEX IF NOT EXISTS idx_email ON users (email);

CREATE TABLE IF NOT EXISTS apps (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    secret TEXT NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS levels (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    expected_input TEXT,
    start_input TEXT
);

CREATE TABLE IF NOT EXISTS hints (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    level_id INTEGER NOT NULL REFERENCES levels(id) ON DELETE CASCADE,
    hint_text TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS user_levels (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER REFERENCES users(id) ON DELETE CASCADE,
    level_id INTEGER REFERENCES levels(id) ON DELETE CASCADE,
    is_completed BOOLEAN DEFAULT FALSE,
    last_input TEXT,
    attempt_response TEXT,
    attempts INTEGER DEFAULT 0
);

-- ========== ЗАПОЛНЕНИЕ УРОВНЕЙ ==========

INSERT INTO levels (id, name, description, expected_input) VALUES
(1, 'SQL Injection: Авторизация без пароля', 'Ваша задача — войти в систему, зная только имя пользователя. Примените SQL-инъекцию.', "' OR '1'='1"),
(2, 'SQL Injection: Получение данных всех пользователей', 'Получите все записи из таблицы пользователей.', "' OR 1=1 --"),
(3, 'SQL Injection: Bypass фильтрации', 'Обойдите фильтр по username, применив вложенную SQL-инъекцию.', "' UNION SELECT null, sqlite_version(), null --"),
(4, 'Memory Out: Неверное использование памяти', 'Ваша задача — вызвать ошибку переполнения памяти, манипулируя памятью в приложении.', "large_string_repeat"),
(5, 'Memory Out: Бесконечная рекурсия', 'Напишите код, который вызывает рекурсию без условия выхода, тем самым приводя к переполнению стека.', "recursive_function()"),
(6, 'Memory Out: Ошибка выделения памяти', 'Создайте утечку памяти, которая приведет к истощению доступной памяти на сервере.', "malloc_overflow"),
(7, 'Linux Sudo Exploit: Получение прав администратора', 'Попробуйте использовать уязвимость в sudo для получения root-доступа.', "sudo vulnerable_command"),
(8, 'Linux Sudo Exploit: Подмена переменных окружения', 'Подмените переменные окружения в sudo, чтобы получить доступ к запрещенной команде.', "env_vars_tampering"),
(9, 'Linux Sudo Exploit: Уязвимость в sudoers', 'Найдите и используйте уязвимость в конфигурации sudoers для повышения привилегий.', "sudoers_injection"),
(10, 'SQL Injection: Ремонт базы данных', 'Вставьте SQL-запрос, который позволит изменить схему базы данных.', "'; DROP TABLE users; --"),
(11, 'Memory Out: Эксплойтим переполнение буфера', 'Попробуйте воспользоваться переполнением буфера для того, чтобы выполнить произвольный код.', "buffer_overflow"),
(12, 'Linux Sudo Exploit: Восстановление доступа к root', 'Используйте уязвимость для восстановления доступа к root-пользователю в системе.', "sudo_revocation"),
(13, 'SQL Injection: Захват сессии', 'Захватите сессию другого пользователя, применив SQL-инъекцию в таблице сессий.', "' OR session_id = 'target_session_id"),
(14, 'Memory Out: Чтение из освобожденной памяти', 'Попробуйте обратиться к памяти после её освобождения для получения уязвимости.', "use_after_free"),
(15, 'Linux Sudo Exploit: Окружение как оружие', 'Используйте sudo и переменные окружения для выполнения команды от имени root.', "sudo env var");

-- ========== ПОДСКАЗКИ ДЛЯ УРОВНЕЙ ==========
INSERT INTO hints (level_id, hint_text) VALUES
(1, 'Попробуй использовать условие, которое всегда истинно.'),
(1, "SQL использует одинарные кавычки для строк."),
(1, "Заверши выражение, чтобы обмануть WHERE условие."),
(2, 'Посмотри, как можно обойти фильтрацию на сервере.'),
(2, 'Попробуй объединить запросы с помощью UNION.'),
(2, "Не забывай обрабатывать результат запросов."),
(3, 'Ищи возможность добавить свой запрос к уже существующему.'),
(3, 'Проверьте значение на правильность в SQL-запросах.'),
(3, 'Понять, где могут быть уязвимости с UNION запросами.'),
(4, 'Попробуйте заполнить память, создавая большие объекты.'),
(4, 'Вызов функции с большим количеством данных может привести к ошибке.'),
(4, "Необходимо изучить память приложения и её использование."),
(5, 'Поищи место, где функция вызывает сама себя без остановки.'),
(5, "Рекурсия может привести к переполнению стека, если нет ограничения."),
(5, 'Помни, что рекурсивные вызовы без базового случая приведут к ошибке.'),
(6, 'Проверьте, как работает выделение памяти в приложении.'),
(6, 'Подумай, как можно переполнить память через массивы или структуры.'),
(6, "Смотрите, как управляются ресурсы памяти в приложении."),
(7, 'Посмотри, какие команды могут быть выполнены через sudo.'),
(7, 'Проверьте настройки sudo для получения прав root.'),
(7, 'Используйте уязвимости в sudo для повышения привилегий.'),
(8, 'Подмена переменных окружения может дать доступ к другим командам.'),
(8, 'Изучи возможности подмены окружения в sudo.'),
(8, 'Использование переменных окружения может изменить поведение sudo.'),
(9, 'Обратите внимание на конфигурацию sudoers для уязвимостей.'),
(9, 'Проверьте возможность выполнения команд с правами суперпользователя.'),
(9, "Не забудьте изучить возможные ошибки конфигурации sudoers."),
(10, 'SQL-инъекции могут использоваться для удаления данных из базы.'),
(10, 'Посмотри, как можно изменить запросы к базе данных.'),
(10, 'Заверши SQL-запрос и добавь команду удаления таблицы.'),
(11, 'Подумай, как использовать переполнение буфера для выполнения кода.'),
(11, 'Изучи переполнение памяти и переполнение буфера.'),
(11, 'Тщательно следите за буферами и их размерами.'),
(12, 'Изучи возможности восстановления доступа через sudo.'),
(12, 'Попробуй найти способ восстановления прав root.'),
(12, 'Использование sudo позволяет вернуть доступ к системе.'),
(13, 'Посмотри на таблицу сессий и её уязвимости.'),
(13, 'Проверьте, как SQL-инъекция может использоваться для захвата сессий.'),
(13, 'Ищи уязвимости в механизме сессий.'),
(14, 'Работа с памятью после её освобождения может вызвать ошибки.'),
(14, 'Попробуй обратиться к данным после их удаления.'),
(14, 'Изучи работу с памятью в приложении, когда объекты уничтожаются.'),
(15, 'Переменные окружения могут стать оружием для выполнения команд.'),
(15, 'Использование sudo с переменными окружения может дать доступ к root.'),
(15, 'Смотри, как можно использовать sudo с переменными окружения.');

EOF

echo "✅ База данных успешно создана и заполнена."

